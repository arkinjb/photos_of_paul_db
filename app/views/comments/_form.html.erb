<%= form_for [@photo, @comment] do |f| %>
<% if @comment.new_record? %>
  <%= f.label :content, "Add a New Comment" %>
<% else %>
  <%= f.label :content, "Edit Your Comment" %>
<% end %>
<%= f.text_area :content %>
<%= f.hidden_field :photo_id, value: @photo.id %>

<%= f.submit %>
<% end %>
