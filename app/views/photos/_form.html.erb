

<%= form_for(@photo, multipart: true) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <% if @photo.new_record? %>

    <%= f.label :image %>
    <%= f.file_field :image %>
    <%= f.submit "Upload Photo" %>

  <% else %>
    <%= f.submit "Edit Title" %>
  <% end %>

<% end %>
