<%= form_for @photo do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :photo_url %>
  <%= f.text_field :photo_url %>

  <% if @photo.new_record? %>
    <%= f.submit "Upload Photo" %>
  <% else %>
    <%= f.submit "Edit Photo" %>
  <% end %>

<% end %>
